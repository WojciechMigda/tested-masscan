debug protocols all;
debug commands 2;

protocol device {
}

protocol direct {
  interface "eth0";
  interface "eth1";
}

protocol static {
  route 10.9.0.0/24 via 10.9.0.254 blackhole;
}

protocol kernel {
  import none;
  export all;
}

protocol bgp {
  import all;
#  export where source = RTS_DEVICE;
#  export where source ~ [ RTS_STATIC, RTS_BGP ];
  export all;

  local    10.100.0.9 as 9;
  neighbor 10.100.0.7 as 7;
}
