protocol device {
}

protocol direct {
  interface "eth0";
}

protocol kernel {
  import none;
  export all;
}

protocol bgp {
  import all;
  export all;

  local    10.1.0.253 as 5;
  neighbor 10.1.0.1   as 1;
}
